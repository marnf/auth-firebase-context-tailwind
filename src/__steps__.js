/**
 * CONTEXT API
 * 1.create context with (createContext) and export it
 * 2.create provider
 * 3.set context value
 * 4.access the value with (useContext) hook
 */

/**
 * 1.set AuthProvider in main.jsx
 * 2.use children props for access the input file 
 * 3.give 
 */